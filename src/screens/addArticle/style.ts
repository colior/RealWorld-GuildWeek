import {StyleSheet} from 'react-native';

const AddArticleScreenStyle = StyleSheet.create({
  container: {
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 40,
  },
  titleContainer: {
    marginBottom: 20,
  },
  title: {
    fontSize: 40,
  },
});

export default AddArticleScreenStyle;
